version: 1
dataset: products
keys: [product_id]
expectations:
  product_id_not_null: {severity: fail, expression: "product_id IS NOT NULL"}
  non_negative_price: {severity: quarantine, expression: "unit_price >= 0"}
