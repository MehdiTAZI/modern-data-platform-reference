# Security Standard
No secrets in source. Use managed identity/OIDC/M2M. Grant groups rather than users. Production ownership belongs to controlled groups/service principals. External cloud paths must be governed through Unity Catalog storage credentials/external locations/volumes.
