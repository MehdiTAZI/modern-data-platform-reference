# Engineering Standard
Prefer reusable package code over notebook-only logic; deterministic transforms; explicit schemas/contracts; idempotent/replayable ingestion; structured metadata; automated tests; code review; infrastructure/app lifecycle separation.
