# Naming Standard
Catalog: `retail_<dev|stg|prd>`. Schemas: `bronze`, `silver`, `gold`, `ops`. Azure resources use `<project>-<env>-<type>` where provider constraints allow. Service principals/groups express domain + role, never personal names.
