# Disaster Recovery

IaC reconstructs platform/governance configuration; object-storage durability protects persisted lake data; source retention plus Bronze replay supports pipeline recovery. Checkpoints are acceleration state, not the only copy of business data. RPO/RTO are reference targets in the NFR document and must be recalibrated per organization/region.
