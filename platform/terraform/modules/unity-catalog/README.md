# Unity Catalog Module

Planned resources:

- storage credentials / external locations;
- catalog and schema baseline;
- workspace bindings where applicable;
- group ownership and grants;
- external volumes for landing zones.

This module is intentionally introduced after the workspace/storage foundation so its credentials and isolation model can be documented before implementation.
