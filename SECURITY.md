# Security Policy

This repository is a reference implementation and must not contain production credentials or customer data.

For a security issue, use a private GitHub security advisory rather than a public issue when possible. Do not include live secrets in reports.

Supported branch: `main`.
